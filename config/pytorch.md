---
slug: pytorch
languages: [cpp, python]
repo: /mnt/disks/persist/torch-tpu/pytorch_src
compile_commands: build/compile_commands.json
index_shards: ["torch/*"]
tests: ["test/**/*.py"]
docs: ["**/README*.md", "docs/**/*.md"]
---

# pytorch — ingest config

Mixed C++/Python. The C++ core is indexed by scip-clang against
`build/compile_commands.json` (generated by a cmake configure) and merged with the
scip-python index. Agenda is discovery-driven.

## Concepts
<!-- discovery-driven -->
